# Old Android project (kept for reference, not built)
app/build/
build/
.gradle/
local.properties

# Samples — reference files, not for the repo
samples/

# Local scratch files
tmp/

# Signing keys (never commit)
flutter_app/android/key.properties
flutter_app/android/app/release-keystore.jks

# Flutter build artifacts (flutter_app has its own .gitignore)
flutter_app/build/
flutter_app/.dart_tool/
flutter_app/.flutter-plugins
flutter_app/.flutter-plugins-dependencies
